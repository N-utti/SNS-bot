# 백엔드 설정 (docker-compose 는 자체 환경변수로 덮어쓰므로 로컬 직접 실행용)
DATABASE_URL=postgres://sns:sns@localhost:5432/sns
APP_ENV=dev

# M1 에서 추가 예정:
# FERNET_KEY=            # sns_accounts 자격증명 암호화 키 (base64, 32바이트)
# THREADS_APP_ID=
# THREADS_APP_SECRET=
# NAVER_CLIENT_ID=
# NAVER_CLIENT_SECRET=
